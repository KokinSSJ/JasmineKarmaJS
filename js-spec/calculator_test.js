describe("Calculator test", function() {
//	it("add 3 and 4", function() {
//		expect(dc.add(3, 4)).toBe(7);
//	});
	it("and so is a spec", function() {
	    a = true;

	    expect(a).toBe(true);
	  });
	it("and so is a spec2", function() {
	    a = true;

	    expect(a).toBe(true);
	  });
	it("and so is a spec3", function() {
	    a = true;

	    expect(a).toBe(true);
	  });
})
